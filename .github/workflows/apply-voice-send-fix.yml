name: apply-voice-send-fix
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Write and run fix script
        run: |
          python3 - <<'PY'
          import base64
          from pathlib import Path
          b64 = open('.github/workflows/apply-voice-send-fix.yml').read()  # placeholder
          print('will be replaced')
          PY
