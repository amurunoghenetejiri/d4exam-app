name: Revert officer hysteresis
# Disabled one-shot patch workflow — manual only. Does not block CI.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled (no-op)
        run: echo "This one-shot workflow is disabled. Use Build Android APK / Release Android APK."
