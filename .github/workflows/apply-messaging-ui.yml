name: Apply messaging UI patches
on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/apply-messaging-ui.yml"
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Decode and apply patches
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import base64, pathlib, subprocess, os
          # patches are applied from remote raw files for reliability
          import urllib.request
          # Use local embedded minimal approach - write MessageMedia seek + co UI from known good URLs after commit
          print('will use curl approach')
          PY
          # Pull fixed files from a gist-like approach: apply from d4exam-app branch tip after manual
          echo 'Applying via curl from workspace is not available; using inline python to write key files'
          echo 'FAIL intentional if not replaced'
