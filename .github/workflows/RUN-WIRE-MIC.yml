name: RUN-WIRE-MIC
# One-shot / historical patch workflow — manual only. Does not block main.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled (no-op)
        run: echo "Disabled one-shot workflow. Core CI is Build/Release Android APK."
